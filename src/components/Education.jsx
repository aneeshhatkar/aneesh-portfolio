import SectionHeader from "./SectionHeader";

export default function Education() {
  return (
    <section id="education" className="px-6 py-28">
      <SectionHeader
        eyebrow="Education"
        title="Education"
        description="Academic background in machine learning, AI systems, robotics, and computer vision."
      />
    </section>
  );
}